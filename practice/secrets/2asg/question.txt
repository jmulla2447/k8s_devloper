Scenario:

Your company's security policy requires that all database passwords used by applications are stored securely. You have an existing application that requires access to a MySQL database and needs the database password stored as a Secret.

Task:

Create a Secret named db-password with the following data:

Key: password
Value: your-mysql-db-password-here
Image: mysql:latest
